
const initialState = {
    current: null,
    previous: [],
    future: [],
    responses: {}
}

function answers(state = initialState, action) {
    switch(action.type) {
        default:
            return initialState;
    }
}

export default answers;